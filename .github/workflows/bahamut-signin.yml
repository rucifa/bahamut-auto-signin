name: Bahamut 自動簽到

on:
  schedule:
    - cron: '5 20 * * *'
  workflow_dispatch:

jobs:
  signin:
    runs-on: ubuntu-latest
    
    steps:
      - name: 檢出代碼
        uses: actions/checkout@v4
      
      - name: 設定 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: 安裝 Python 依賴
        run: |
          pip install -r requirements.txt
      
      - name: 安裝 Chrome 和 ChromeDriver
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser chromium-chromedriver
          sudo ln -s /usr/lib/chromium-browser/chromium /usr/bin/google-chrome || true
          sudo ln -s /usr/bin/chromedriver /usr/local/bin/chromedriver || true
      
      - name: 執行簽到
        env:
          BAHAMUT_USERNAME: ${{ secrets.BAHAMUT_USERNAME }}
          BAHAMUT_PASSWORD: ${{ secrets.BAHAMUT_PASSWORD }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER || 'smtp.gmail.com' }}
          SMTP_PORT: ${{ secrets.SMTP_PORT || '587' }}
        run: python bahamut_signin.py
      
      - name: 工作流程完成
        if: always()
        run: echo "簽到工作流程已完成"
